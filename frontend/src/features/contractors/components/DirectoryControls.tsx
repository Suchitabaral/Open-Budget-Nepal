import { Search, SlidersHorizontal } from "lucide-react";
import { Input } from "@/components/ui/input";
import type { DirectoryFilters, DirectoryMetadata } from "@/features/contractors/api/contractorDirectoryApi";

type Props={filters:DirectoryFilters;metadata:DirectoryMetadata|null;onChange:(key:keyof DirectoryFilters,value:string|number)=>void};
export default function DirectoryControls({filters,metadata,onChange}:Props){return <div className="border-b border-slate-200 bg-white p-4"><div className="flex items-center gap-2 text-sm font-semibold text-slate-900"><SlidersHorizontal className="h-4 w-4 text-emerald-700"/>Search and filters</div><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-4"><label className="relative xl:col-span-2"><span className="sr-only">Contractor name</span><Search className="absolute left-3 top-3 h-4 w-4 text-slate-500"/><Input value={filters.search} onChange={e=>onChange("search",e.target.value)} className="pl-9" placeholder="Search contractor name"/></label><label><span className="sr-only">PAN</span><Input value={filters.pan} inputMode="numeric" onChange={e=>onChange("pan",e.target.value.replace(/\D/g,""))} placeholder="Search PAN"/></label><Filter value={filters.category} label="All contract categories" options={metadata?.categories??[]} onChange={value=>onChange("category",value)}/></div><div className="mt-3 flex flex-wrap gap-3"><Filter value={filters.fiscalYear} label="All contract fiscal years" options={metadata?.fiscalYears??[]} onChange={value=>onChange("fiscalYear",value)}/><Filter value={filters.sort} label="Name A–Z" options={["name_desc"]} optionLabels={{name_desc:"Name Z–A"}} onChange={value=>onChange("sort",value)}/></div></div>}
function Filter({value,label,options,onChange,optionLabels={}}:{value:string;label:string;options:string[];onChange:(value:string)=>void;optionLabels?:Record<string,string>}){return <select value={value} onChange={e=>onChange(e.target.value)} className="h-10 min-w-44 rounded-lg border border-slate-300 bg-white px-3 text-sm text-slate-800 outline-none focus:border-emerald-600 focus:ring-2 focus:ring-emerald-600/15"><option value="all">{label}</option>{options.map(option=><option key={option} value={option}>{optionLabels[option]??option}</option>)}</select>}
